name: Deploy React CSR
on: 
  push:
    branches:
      - main
      - feature/**
      - "!feature/login"
  workflow_dispatch:
  pull_request:
    types:
      - opened
#env:
#  node-version: '22.15.1'
jobs:
  build:
    # environment : Prod
    runs-on: ubuntu-latest
    steps:
      - name: Print node version
        run: echo "Node version:${{vars.NODE_VERSION}}"
      - name: Print API key
        run: echo "API Key:${{secrets.API_KEY}}"
      - name: Clone repo
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{vars.NODE_VERSION}}
      - name: Get npm cache directory
        id: npm-cache-dir
        shell: bash
        run: echo "dir=$(npm config get cache)" >> ${GITHUB_OUTPUT}
      - uses: actions/cache@v4
        id: npm-cache # use this to check for `cache-hit` ==> if: steps.npm-cache.outputs.cache-hit != 'true'
        with:
          path: ${{ steps.npm-cache-dir.outputs.dir }}
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: List dist/
        run: ls dist
      - name: Upload dist
        uses: actions/upload-artifact@v4
        with:
          name: dist-folder
          path: dist/
  deploy:
    if: success()
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - name: Download dist
        uses: actions/download-artifact@v4
        with:
          name: dist-folder
          path: dist
      - name: Deploy
        run: echo "Deploying..."
  notify:
    runs-on: ubuntu-latest
    needs:
      - build
    if: always()
    steps:
      - name: Notify to Discord Text Channel
        env:
          DISCORD_WEBHOOK: https://discord.com/api/webhooks/1421697869458706533/W2UqZa7gD95EU7ixa4gQN3QLz3pWc3oFepvpv16XWxfpuMMTTiqbNULZWrBBmPfPlYBY
        uses: Ilshidur/action-discord@master
        with:
          args: 'Build ${{github.sha}} :( By Gjee Handsome and cool'


        
